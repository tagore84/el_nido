{
    "type": "object",
    "properties": {
        "date": {
            "type": "string",
            "description": "ISO date string (YYYY-MM-DD) for the event"
        },
        "time": {
            "type": [
                "string",
                "null"
            ],
            "description": "Time in HH:MM format (24h), or null if not specified"
        },
        "description": {
            "type": "string",
            "description": "Concise description of the event"
        },
        "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Confidence in the extraction"
        },
        "reasoning": {
            "type": "string",
            "description": "Brief explanation of interpretation"
        }
    },
    "required": [
        "date",
        "description",
        "confidence",
        "reasoning"
    ]
}